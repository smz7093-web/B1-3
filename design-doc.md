# 프로젝트 2 설계 문서

## 1. 프로젝트 개요

- **프로젝트명**: Google Sheets 새 행 추가 시 우선순위별 알림 자동화
- **작성자**: 장상민
- **작성일**: 2026-07-25
- **선택한 자동화 도구**: Make
- **자동화할 반복 업무**: Google Sheets에 새 요청이 등록되면 우선순위에 따라 다른 알림을 보내고 처리 결과를 기록하는 업무

---

## 2. 문제 정의

### 2-1. 현재 반복 업무 설명
> 현재는 요청이 들어오면 담당자가 직접 Google Sheets를 열어 새 행을 확인하고, 우선순위에 따라 이메일 또는 메신저로 알림을 보내고 있다.

- **현재 반복 업무**:
  - 새 요청이 등록되었는지 수시로 시트를 확인한다.
  - 요청의 우선순위를 직접 확인한다.
  - 긴급 요청과 일반 요청을 구분하여 각각 다른 내용으로 알림을 보낸다.
  - 알림을 보낸 뒤 처리 상태를 다시 시트에 수동으로 기록한다.
- **이 업무를 언제 수행하는지**:
  - 요청이 들어올 때마다 수시로 수행한다.
- **얼마나 자주 발생하는지**:
  - 하루 여러 번 반복적으로 발생할 수 있다.
- **수동 처리 시 불편한 점**:
  - 시트를 계속 확인해야 해서 비효율적이다.
  - 긴급 요청을 늦게 확인할 가능성이 있다.
  - 알림 누락이나 상태 기록 누락이 발생할 수 있다.

### 2-2. 자동화가 필요한 이유
- 요청 확인과 알림 전송을 자동화하여 반복 업무를 줄일 수 있다.
- 긴급 요청을 빠르게 전달하여 대응 속도를 높일 수 있다.
- 처리 결과를 자동으로 기록해 업무 누락을 줄일 수 있다.

---

## 3. 목표

### 3-1. 자동화 목표
- Google Sheets에 새 행이 추가되면 자동으로 감지한다.
- `Priority` 값에 따라 긴급/일반 요청을 분기 처리한다.
- 분기 결과에 따라 서로 다른 알림을 자동으로 전송한다.
- 처리 완료 후 상태와 처리 시간을 시트에 자동 기록한다.

### 3-2. 기대 효과
- **처리 시간 단축**: 사람이 직접 확인하고 알림을 보내는 시간을 줄일 수 있다.
- **실수 감소**: 알림 누락 및 상태 기록 누락을 줄일 수 있다.
- **업무 누락 방지**: 새 요청 발생 시 자동 실행되므로 놓칠 가능성이 줄어든다.
- **빠른 알림/응답 가능**: 긴급 요청을 우선적으로 빠르게 전달할 수 있다.

---

## 4. 사용 도구 및 서비스

### 4-1. 자동화 도구
- **도구명**: Make
- **선택 이유**:
  - 시나리오 흐름을 시각적으로 보기 쉽다.
  - Router를 이용한 조건 분기가 직관적이다.
  - 실행 로그 확인이 편해 테스트와 디버깅에 유리하다.
- **무료 플랜 사용 가능 여부**: 가능

### 4-2. 연동 서비스
- **서비스 1**: Google Sheets
- **서비스 2**: Gmail
- **서비스 3**: Make

---

## 5. 워크플로우 설계

## 5-1. 전체 흐름 요약
1. **Trigger 발생**: Google Sheets에 새 행이 추가된다.
2. **데이터 확인 또는 수집**: 요청 제목, 우선순위, 요청자 이메일, 요청 내용을 읽어온다.
3. **조건 분기**: `Priority` 값이 `긴급`인지 `일반`인지 확인한다.
4. **조건 A일 때 실행할 Action**: 긴급 알림 이메일 전송
5. **조건 B일 때 실행할 Action**: 일반 알림 이메일 전송
6. **결과 기록 또는 알림 전송**: 처리 상태와 처리 시간을 Google Sheets에 업데이트한다.

---

## 5-2. 구성 요소 상세

### Trigger
- **종류**: Google Sheets - Watch New Rows
- **동작 방식**:
  - 지정한 스프레드시트와 시트에서 새 행이 추가되면 자동으로 감지한다.
- **예시 데이터**:
  - ID: 1
  - Request Title: 로그인 오류 문의
  - Priority: 긴급
  - Requester Email: user1@example.com
  - Details: 로그인 불가 문제 발생

### Action 1
- **이름**: Gmail - Send an Email
- **역할**:
  - 우선순위에 따라 다른 제목과 내용으로 이메일 알림을 전송한다.
- **입력값**:
  - 요청 제목
  - 우선순위
  - 요청자 이메일
  - 요청 내용
- **출력값**:
  - 이메일 전송 성공 여부

### Action 2
- **이름**: Google Sheets - Update a Row
- **역할**:
  - 자동화가 정상 처리되었음을 시트에 기록한다.
- **입력값**:
  - Row Number 또는 Row ID
  - Status
  - Processed At
- **출력값**:
  - 업데이트 완료된 행 정보

### 조건 분기(Filter / Router)
- **분기 기준**: `Priority` 컬럼 값
- **조건 A**: `Priority = 긴급`
- **조건 B**: `Priority = 일반`

### 추가 Action(있다면)
- **이름**: 없음
- **역할**: 기본 요구사항 충족을 위해 Trigger 1개, Action 2개, 조건 분기 1개 중심으로 구성

---

## 5-3. 분기별 동작 설명

### 분기 A
- **실행 조건**: `Priority = 긴급`
- **수행 작업**:
  1. 제목 앞에 `[긴급]`을 붙인 이메일을 발송한다.
  2. 시트의 `Status`를 `긴급 알림 완료`로 업데이트한다.
  3. `Processed At`에 처리 시각을 기록한다.
- **기대 결과**:
  - 긴급 요청이 즉시 알림 처리되고, 시트에 실행 결과가 남는다.

### 분기 B
- **실행 조건**: `Priority = 일반`
- **수행 작업**:
  1. 제목 앞에 `[일반]`을 붙인 이메일을 발송한다.
  2. 시트의 `Status`를 `일반 알림 완료`로 업데이트한다.
  3. `Processed At`에 처리 시각을 기록한다.
- **기대 결과**:
  - 일반 요청이 자동으로 분류되어 알림 처리되고, 시트에 실행 결과가 남는다.

---

## 6. 입력/출력 데이터 설계

| 항목 | 설명 | 예시 |
|---|---|---|
| 입력 데이터 1 | 요청 제목 | 로그인 오류 문의 |
| 입력 데이터 2 | 요청자 이메일 | user1@example.com |
| 조건 판별 값 | 우선순위 | 긴급 |
| 출력 데이터 1 | 처리 상태 | 긴급 알림 완료 |
| 출력 데이터 2 | 처리 시간 | 2026-07-25 14:30 |

---

## 7. 자동 실행 구조

- **자동 실행 방식**:
  - Google Sheets에 새 행이 추가되면 Make 시나리오가 자동 실행된다.
- **Trigger가 발생하는 조건**:
  - 지정한 시트에 새로운 요청 행이 입력될 때
- **사용자가 수동으로 해야 하는 부분이 있는지**:
  - 최초 시트 작성과 테스트 데이터 입력만 필요하다.
- **완전 자동화 여부**:
  - 요청 감지부터 알림 전송, 상태 기록까지 자동화 가능

---

## 8. 테스트 계획

## 8-1. 테스트 목적
- Trigger가 정상적으로 실행되는지 확인
- 조건 분기가 정상 동작하는지 확인
- 각 Action이 정상적으로 수행되는지 확인
- 분기별 결과가 실제로 생성되는지 확인

## 8-2. 테스트 시나리오

| 테스트 항목 | 입력 조건 | 기대 결과 | 실제 결과 |
|---|---|---|---|
| Trigger 테스트 | 시트에 새 행 추가 | 시나리오 자동 실행 |  |
| 분기 A 테스트 | Priority = 긴급 | 긴급 이메일 전송, 상태 업데이트 |  |
| 분기 B 테스트 | Priority = 일반 | 일반 이메일 전송, 상태 업데이트 |  |
| Action 1 테스트 | 이메일 전송 단계 실행 | Gmail 발송 성공 |  |
| Action 2 테스트 | 시트 업데이트 단계 실행 | Status, Processed At 기록 |  |

---

## 9. 예상 결과

- **자동화가 성공하면**:
  - 새 요청이 등록되자마자 자동으로 감지된다.
  - 요청 우선순위에 따라 다른 알림이 전송된다.
  - 처리 결과가 시트에 자동으로 기록된다.

- **사용자가 얻는 이점**:
  - 시트를 계속 확인하지 않아도 된다.
  - 긴급 요청 대응 속도를 높일 수 있다.
  - 처리 이력을 쉽게 관리할 수 있다.

---

## 10. 구현 후 확인할 화면

- [ ] 전체 워크플로우 화면
- [ ] Trigger 설정 화면
- [ ] 조건 분기 설정 화면
- [ ] Action 설정 화면
- [ ] 실행 로그 화면
- [ ] 결과 화면
- [ ] 분기 A 실행 결과
- [ ] 분기 B 실행 결과

---

## 11. 오류 가능성 및 대응 방법

| 오류 상황 | 원인 | 대응 방법 |
|---|---|---|
| Trigger가 실행되지 않음 | 시트 연결 오류 또는 감시 대상 시트 설정 오류 | 스프레드시트 연결 상태와 시트 이름을 다시 확인 |
| 조건 분기가 예상과 다름 | `Priority` 값 오타 또는 공백 포함 | 입력값을 `긴급`, `일반`으로 통일하고 공백 여부 확인 |
| 메시지 전송 실패 | Gmail 계정 연결 문제 또는 권한 문제 | Gmail 재연결 후 권한 허용 상태 확인 |
| 데이터 저장 실패 | Row ID/Row Number 매핑 오류 | Trigger 출력값에서 정확한 행 번호를 다시 매핑 |

---

## 12. 보안 및 민감정보 처리 계획

- [x] API Key 노출 금지
- [x] 토큰 마스킹 처리
- [x] 이메일 주소 일부 마스킹 처리
- [x] 계정명/워크스페이스 정보 필요 시 가림 처리
- [x] 캡처 이미지 업로드 전 민감정보 재확인

---

## 13. 최종 구현 체크리스트

- [x] Trigger 1개 포함
- [x] Action 2개 이상 포함
- [x] 조건 분기 1개 이상 포함
- [x] 자동 실행 확인
- [x] 분기 A 실제 실행 확인
- [x] 분기 B 실제 실행 확인
- [ ] 실행 로그 캡처 완료
- [ ] 결과 화면 캡처 완료
- [ ] 민감정보 마스킹 완료

---

## 14. 한 줄 요약

> 이 프로젝트는 Google Sheets에 등록되는 요청을 우선순위에 따라 자동 분기하여 알림을 보내고, 처리 상태를 기록함으로써 반복 확인 업무와 알림 누락 문제를 줄이기 위한 자동화 워크플로우이다.

## 폴더정리 

project2/
├─ design-doc.md
└─ screenshots/
   ├─ 01-overall-scenario.png
   ├─ 02-trigger-google-sheets.png
   ├─ 03-router-priority-branch.png
   ├─ 04-action-gmail-urgent.png
   ├─ 05-action-gmail-normal.png
   ├─ 06-action-update-row.png
   ├─ 07-log-urgent-branch.png
   └─ 08-log-normal-branch.png
